#include <stdio.h>
#include <stdlib.h>

void main() {
    // Print a message to the console
    printf("Hello, World!\n");

    // Exit the program with a success status
    exit(EXIT_SUCCESS);
}
// This program prints "Hello, World!" to the console and exits successfully.